{
  "name": "@teamduality/heygen-typescript-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.0.0"
  }
}
